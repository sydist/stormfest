processResources {
	filesMatching("fabric.mod.json") {
		expand project.properties
	}
}

loom {
	accessWidenerPath = file("src/main/resources/${project.mod_id}.accesswidener")
	runs {
		client {
			vmArgs project.vm_args.split(" ")
		}
	}
}